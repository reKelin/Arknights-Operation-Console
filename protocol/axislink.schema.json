{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:axislink:v2",
  "title": "AxisDocument",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "title", "stageId", "timebase", "events"],
  "properties": {
    "schemaVersion": {
      "const": 2
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": ".*\\S.*"
    },
    "stageId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9_./#-]*$"
    },
    "timebase": {
      "$ref": "#/$defs/Timebase"
    },
    "events": {
      "type": "array",
      "items": {
        "oneOf": [
          { "$ref": "#/$defs/DeployEvent" },
          { "$ref": "#/$defs/SkillEvent" },
          { "$ref": "#/$defs/RetreatEvent" }
        ]
      }
    }
  },
  "$defs": {
    "Timebase": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fps"],
      "properties": {
        "fps": {
          "const": 30
        }
      }
    },
    "EventId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$"
    },
    "OperatorId": {
      "type": "string",
      "minLength": 6,
      "maxLength": 128,
      "pattern": "^(char|token)_[A-Za-z0-9_]+$"
    },
    "Frame": {
      "type": "integer",
      "minimum": 0,
      "maximum": 2147483647
    },
    "Label": {
      "type": "string",
      "maxLength": 120
    },
    "Tile": {
      "type": "string",
      "pattern": "^[A-I](?:[1-9]|[12][0-9]|3[0-6])$"
    },
    "Direction": {
      "type": "string",
      "enum": ["up", "right", "down", "left", "none"]
    },
    "DeployEvent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "frame", "kind", "operator", "tile", "direction"],
      "properties": {
        "id": { "$ref": "#/$defs/EventId" },
        "frame": { "$ref": "#/$defs/Frame" },
        "kind": { "const": "deploy" },
        "operator": { "$ref": "#/$defs/OperatorId" },
        "tile": { "$ref": "#/$defs/Tile" },
        "direction": { "$ref": "#/$defs/Direction" },
        "label": { "$ref": "#/$defs/Label" }
      }
    },
    "SkillEvent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "frame", "kind", "tile"],
      "properties": {
        "id": { "$ref": "#/$defs/EventId" },
        "frame": { "$ref": "#/$defs/Frame" },
        "kind": { "const": "skill" },
        "tile": { "$ref": "#/$defs/Tile" },
        "label": { "$ref": "#/$defs/Label" }
      }
    },
    "RetreatEvent": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "frame", "kind", "tile"],
      "properties": {
        "id": { "$ref": "#/$defs/EventId" },
        "frame": { "$ref": "#/$defs/Frame" },
        "kind": { "const": "retreat" },
        "tile": { "$ref": "#/$defs/Tile" },
        "label": { "$ref": "#/$defs/Label" }
      }
    }
  }
}
